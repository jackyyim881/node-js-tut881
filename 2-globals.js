// Modules
const secret = 'super secret'
const john ='john'
const peter = 'peter'

const sayHi = (name) =>{
    console.log(`Hello there ${name}`);
}

sayHi('susan')
sayHi(john)
sayHi(peter)

const yo = (names)=>{

    console.log(`hello${names}`);
}

yo('names')
